# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# What a Playwright run leaves behind: the HTML report, and the traces and
# screenshots of whatever failed. Both are outputs of the e2e/ suite rather
# than sources of it -- CI uploads them as artefacts of the run that produced
# them, which is the only place they mean anything.
playwright-report
test-results

