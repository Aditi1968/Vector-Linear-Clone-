/**
 * The workspace Settings screen.
 *
 *     { path: ROUTE_SEGMENTS.settings, element: <SettingsPage /> }
 *
 * The data layer is ./api.ts and is reachable from `features/settings/api`
 * when a test needs a document to mock, but never through this module.
 */
export { SettingsPage } from './SettingsPage'
