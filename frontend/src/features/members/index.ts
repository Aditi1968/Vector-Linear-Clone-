/**
 * The Members screen.
 *
 *     { path: ROUTE_SEGMENTS.members, element: <MembersPage /> }
 *
 * The data layer is ./api.ts and is reachable from `features/members/api`
 * when a test needs a document to mock, but never through this module.
 */
export { MembersPage } from './MembersPage'
