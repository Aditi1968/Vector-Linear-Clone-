/**
 * The Inbox screen.
 *
 *     { path: ROUTE_SEGMENTS.inbox, element: <InboxPage /> }
 *
 * The data layer is ./api.ts and is reachable from `features/inbox/api` when
 * a test needs a document to mock, but never through this module.
 */
export { InboxPage } from './InboxPage'
